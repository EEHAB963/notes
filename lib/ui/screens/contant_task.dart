import 'package:flutter/material.dart';

class ContantTask extends StatelessWidget {
  const ContantTask({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        TextFormField(),
        TextFormField(),
      ],
    );
  }
}
